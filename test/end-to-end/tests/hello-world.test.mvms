/*---
description: >
  Exports a function with ID 42 that prints "hello-world".
runExportedFunction: 42
expectedPrintout: |
  Hello, World!
---*/
// TODO: It looks like the module object isn't freed by the GC
function run() {
  print('Hello, World!');
}

vmExport(42, run);